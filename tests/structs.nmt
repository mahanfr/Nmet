struct Animal {
    name @str,
    sound @str,
}

func main() {
    ~ var a @Animal = {
    ~     name: "dog",
    ~     sound: "bark",
    ~ };
    var a @Animal;
    a.name = "dog";
    print a.name;
}
